import styled from "styled-components";

import { Container as Wrapper } from "@/presentation/components";

export const Container = styled.div`
  padding: 4rem 0;
`;

export const Content = styled(Wrapper)``;
